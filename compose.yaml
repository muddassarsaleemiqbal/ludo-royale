services:
  online:
    build:
      context: .
      dockerfile: Dockerfile.server
    environment:
      DATABASE_URL: ${DATABASE_URL}
      LUDO_SERVER_ADDR: 0.0.0.0:8080
      LUDO_ALLOWED_ORIGINS: http://localhost:5173
    ports:
      - "8080:8080"
    restart: unless-stopped
